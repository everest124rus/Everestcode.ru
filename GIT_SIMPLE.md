# 🎯 Git ПРОСТЫМИ СЛОВАМИ - Как работать с версиями

## 📦 Что такое Git?

**Git** — это как "машина времени" для вашего кода. Вы можете:
- Сохранять версии кода (как снимки)
- Возвращаться к старым версиям
- Видеть, что изменилось

---

## 🔢 Как создать версию (например, 0.0.2)?

### Шаг 1: Сделали изменения в коде
Например, вы изменили файл `AIPanel.tsx`

### Шаг 2: Сохраните изменения в Git
```bash
# Перейдите в папку проекта
cd /home/everest/Sites/everestcode.ru

# Добавьте все измененные файлы
git add .

# Создайте коммит (сохранение) с названием версии
git commit -m "Версия 0.0.2: исправлена ошибка с цветом текста"
```

### Шаг 3: Создайте тег (метку версии)
```bash
git tag v0.0.2
```

**Готово!** Теперь у вас есть версия 0.0.2

---

## 📝 Пример: Создание версий 0.0.2, 0.0.3, 0.0.4

### Версия 0.0.2:
```bash
# 1. Изменили код
# 2. Сохранили
git add .
git commit -m "Версия 0.0.2: добавлена новая функция"
git tag v0.0.2
```

### Версия 0.0.3:
```bash
# 1. Изменили код еще раз
# 2. Сохранили
git add .
git commit -m "Версия 0.0.3: исправлены баги"
git tag v0.0.3
```

### Версия 0.0.4:
```bash
# 1. Изменили код
# 2. Сохранили
git add .
git commit -m "Версия 0.0.4: обновлен дизайн"
git tag v0.0.4
```

---

## ⏪ Как вернуться к старой версии (например, 0.0.3)?

### Вариант 1: Вернуть один файл из версии 0.0.3
```bash
# Если вы удалили файл и хотите его вернуть из версии 0.0.3
git checkout v0.0.3 -- путь/к/файлу

# Пример:
git checkout v0.0.3 -- everest-ai-editor/src/components/AIPanel.tsx
```

### Вариант 2: Посмотреть, что было в версии 0.0.3
```bash
# Посмотреть список файлов в версии 0.0.3
git show v0.0.3 --name-only

# Посмотреть содержимое файла из версии 0.0.3
git show v0.0.3:путь/к/файлу
```

### Вариант 3: Временно переключиться на версию 0.0.3 (только для просмотра)
```bash
# ВНИМАНИЕ: Это создаст "detached HEAD" - только для просмотра!
git checkout v0.0.3

# Чтобы вернуться обратно:
git checkout main
```

---

## 🔍 Полезные команды

### Посмотреть все версии (теги):
```bash
git tag
```

### Посмотреть историю с версиями:
```bash
git log --oneline --decorate
```

### Посмотреть, что изменилось между версиями:
```bash
git diff v0.0.2 v0.0.3
```

---

## ⚠️ ВАЖНО: Как это работает на самом деле

**Git НЕ работает так:**
- ❌ "Включить версию 0.0.3" — такой команды нет

**Git работает так:**
- ✅ Вы создаете **коммиты** (сохранения) с описанием
- ✅ Вы создаете **теги** (метки) для удобства (v0.0.2, v0.0.3)
- ✅ Вы можете **вернуть файлы** из любого коммита/тега

---

## 🎬 Реальный пример работы

### Сценарий: Вы удалили файлы и хотите их вернуть

```bash
# 1. Посмотрите, какие версии у вас есть
git tag
# Вывод: v0.0.2, v0.0.3

# 2. Посмотрите, что было удалено
git status
# Вывод: "удалено: файл.txt"

# 3. Верните файл из версии 0.0.3
git checkout v0.0.3 -- файл.txt

# 4. Файл вернулся! Теперь сохраните это
git add файл.txt
git commit -m "Восстановлен файл из версии 0.0.3"
```

---

## 📋 Шпаргалка: Типичные ситуации

### Ситуация 1: Создал версию 0.0.2
```bash
git add .
git commit -m "Версия 0.0.2: описание изменений"
git tag v0.0.2
```

### Ситуация 2: Создал версию 0.0.3
```bash
git add .
git commit -m "Версия 0.0.3: описание изменений"
git tag v0.0.3
```

### Ситуация 3: Удалил файлы и хочу вернуть из 0.0.3
```bash
# Вернуть конкретный файл
git checkout v0.0.3 -- путь/к/файлу

# Или вернуть все файлы (ОСТОРОЖНО!)
git checkout v0.0.3 -- .
```

### Ситуация 4: Хочу посмотреть, что было в версии 0.0.2
```bash
git show v0.0.2
```

---

## 💡 Главное правило

**Git сохраняет ВСЕ изменения.** Вы не "включаете" версию, а:
1. **Создаете коммит** (сохранение) с описанием
2. **Создаете тег** (метку) для удобства (v0.0.2, v0.0.3)
3. **Возвращаете файлы** из нужного коммита/тега при необходимости

---

## 🚀 Быстрый старт

```bash
# Всегда начинайте с перехода в папку проекта
cd /home/everest/Sites/everestcode.ru

# 1. Посмотреть статус
git status

# 2. Сохранить изменения
git add .
git commit -m "Версия 0.0.2: что вы сделали"

# 3. Создать тег версии
git tag v0.0.2

# 4. Посмотреть все версии
git tag
```

---

**Теперь вы понимаете! Git — это не "включение версий", а сохранение и восстановление файлов из истории.** 🎉

