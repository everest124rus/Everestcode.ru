# 📚 Руководство по Git для начинающих

## ✅ Что уже сделано

1. ✅ Git установлен и настроен
2. ✅ Репозиторий инициализирован
3. ✅ Создан первый коммит со всеми файлами проекта

---

## 🎯 Основные понятия

**Git** — система контроля версий, которая сохраняет историю изменений вашего кода.

**Репозиторий** — папка с вашим проектом, в которой Git отслеживает изменения.

**Коммит** — сохранение текущего состояния файлов с описанием изменений.

**Ветка (branch)** — отдельная линия разработки. По умолчанию используется ветка `main`.

---

## 📝 Базовые команды Git

### 1. Проверить статус репозитория
```bash
git status
```
Показывает, какие файлы изменены, добавлены или удалены.

### 2. Посмотреть изменения в файлах
```bash
git diff
```
Показывает, что именно изменилось в файлах.

### 3. Добавить файлы в индекс (подготовить к коммиту)
```bash
# Добавить все измененные файлы
git add .

# Добавить конкретный файл
git add путь/к/файлу

# Пример
git add everest-ai-editor/src/components/AIPanel.tsx
```

### 4. Создать коммит (сохранить изменения)
```bash
git commit -m "Описание того, что было изменено"
```

**Примеры хороших сообщений коммита:**
- `git commit -m "Исправлена ошибка с CORS в API"`
- `git commit -m "Добавлена новая функция авторизации"`
- `git commit -m "Обновлены стили для AI панели"`

### 5. Посмотреть историю коммитов
```bash
git log
```

Короткий вариант:
```bash
git log --oneline
```

### 6. Отменить изменения в файле (до добавления в индекс)
```bash
git checkout -- путь/к/файлу
```

### 7. Убрать файл из индекса (но оставить изменения)
```bash
git reset HEAD путь/к/файлу
```

---

## 🔄 Типичный рабочий процесс

### Когда вы внесли изменения в код:

1. **Проверьте, что изменилось:**
   ```bash
   git status
   ```

2. **Посмотрите детали изменений (опционально):**
   ```bash
   git diff
   ```

3. **Добавьте файлы в индекс:**
   ```bash
   git add .
   ```
   или
   ```bash
   git add конкретный_файл
   ```

4. **Создайте коммит:**
   ```bash
   git commit -m "Краткое описание изменений"
   ```

---

## 📋 Полезные команды

### Посмотреть текущую ветку
```bash
git branch
```

### Создать новую ветку
```bash
git branch название_ветки
```

### Переключиться на ветку
```bash
git checkout название_ветки
```

### Посмотреть, какие файлы игнорируются
```bash
cat .gitignore
```

---

## ⚠️ Важные моменты

1. **`.gitignore`** — файл, который указывает Git, какие файлы НЕ нужно отслеживать (например, `node_modules`, базы данных, временные файлы).

2. **Не коммитьте:**
   - Файлы с паролями и секретами (`.env` с реальными данными)
   - Большие бинарные файлы
   - Временные файлы

3. **Коммитьте часто** — лучше делать много маленьких коммитов, чем один большой.

4. **Пишите понятные сообщения коммитов** — через месяц вы сами не вспомните, что означало "fix".

---

## 🚀 Примеры использования

### Пример 1: Вы исправили ошибку
```bash
git status                    # Проверить статус
git add everest-ai-editor/src/components/AIPanel.tsx
git commit -m "Исправлена ошибка с белым цветом текста в AI панели"
```

### Пример 2: Вы добавили новую функцию
```bash
git add .
git commit -m "Добавлена функция отправки файлов в Telegram"
```

### Пример 3: Вы хотите отменить последние изменения в файле
```bash
git checkout -- everest-ai-editor/src/components/AIPanel.tsx
```

---

## 📖 Дополнительные ресурсы

- [Официальная документация Git](https://git-scm.com/doc)
- [Интерактивный учебник Git](https://learngitbranching.js.org/)
- [GitHub Guides](https://guides.github.com/)

---

## 💡 Советы

1. **Используйте `git status` часто** — это поможет понять, что происходит с вашими файлами.

2. **Делайте коммиты логически** — группируйте связанные изменения вместе.

3. **Не бойтесь экспериментировать** — Git позволяет откатить изменения, если что-то пошло не так.

4. **Изучайте постепенно** — начните с базовых команд, остальное придет с опытом.

---

**Удачи в работе с Git! 🎉**

