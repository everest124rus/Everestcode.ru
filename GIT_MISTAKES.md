# ❌ Частые ошибки и как их исправить

## Ошибка 1: `git add 0.0.2`
**Проблема:** `0.0.2` — это не файл, а название версии

**Правильно:**
```bash
# Добавить все измененные файлы
git add .

# Или конкретный файл
git add everest-ai-editor/src/components/AIPanel.tsx
```

---

## Ошибка 2: `git commit` когда нет изменений
**Проблема:** Git говорит "нечего коммитить"

**Причина:** Вы не изменили никакие файлы

**Решение:** 
1. Сначала измените код (отредактируйте файл)
2. Потом делайте `git add .` и `git commit`

---

## Ошибка 3: `git teg` (опечатка)
**Проблема:** Опечатка в команде

**Правильно:**
```bash
git tag v0.0.2
```

---

## Ошибка 4: `fatal: метка «v0.0.2» уже существует`
**Проблема:** Тег с таким именем уже есть

**Решение:**
```bash
# Посмотреть все теги
git tag

# Использовать другое имя, например:
git tag v0.0.4

# Или удалить старый тег и создать новый
git tag -d v0.0.2
git tag v0.0.2
```

---

## ✅ Правильная последовательность действий

### Пример: Создание версии 0.0.5

```bash
# 1. Перейдите в папку проекта
cd /home/everest/Sites/everestcode.ru

# 2. ИЗМЕНИТЕ КОД (отредактируйте файл в редакторе)
# Например, изменили файл AIPanel.tsx

# 3. Проверьте, что изменилось
git status
# Должно показать: "изменено: everest-ai-editor/src/components/AIPanel.tsx"

# 4. Добавьте изменения
git add .

# 5. Создайте коммит
git commit -m "Версия 0.0.5: исправлена ошибка с цветом"

# 6. Создайте тег
git tag v0.0.5

# 7. Проверьте
git tag
# Должно показать: v0.0.2, v0.0.3, v0.0.4, v0.0.5
```

---

## 🔍 Как проверить, есть ли изменения

```bash
# Проверить статус
git status

# Если показывает "нечего коммитить" - значит нет изменений
# Если показывает список файлов - есть изменения
```

---

## 💡 Важно помнить

1. **Сначала меняете код** → потом коммитите
2. **`git add`** добавляет файлы, а не версии
3. **`git tag`** создает метку версии (не `teg`!)
4. **Теги должны быть уникальными** — если тег существует, используйте другое имя

---

## 🎯 Быстрая проверка перед коммитом

```bash
# 1. Проверьте статус
git status

# 2. Если есть изменения - добавьте их
git add .

# 3. Создайте коммит
git commit -m "Версия X.X.X: описание"

# 4. Создайте тег
git tag vX.X.X
```

---

**Главное правило: Git сохраняет ИЗМЕНЕНИЯ в файлах, а не просто версии!** 📝

